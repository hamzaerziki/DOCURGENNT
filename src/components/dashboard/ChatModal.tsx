// This component has been removed as direct contact between senders and travelers 
// is no longer allowed in the platform workflow.
// All communication is now handled through the platform's secure relay system.

export const ChatModal = () => {
  return null;
};